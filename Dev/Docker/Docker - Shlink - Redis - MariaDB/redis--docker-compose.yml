services:
  redis:
    image: redis:latest
    container_name: redis
    restart: always
    networks:
      - VMK_NETWORK_01
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes", "--requirepass", "9999"]
    volumes:
      - redis:/data

volumes:
  redis:

networks:
  VMK_NETWORK_01:
    external: true
