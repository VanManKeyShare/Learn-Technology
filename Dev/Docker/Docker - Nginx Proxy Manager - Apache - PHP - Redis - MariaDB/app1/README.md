# DOCKER - APACHE - PHP - REDIS - MARIADB

## 1. CREATE DOCKER COMPOSE - APACHE - PHP - REDIS - MARIADB

```bash
docker compose -p "apache-php-redis-mariadb" -f apache-php-redis-mariadb--docker-compose.yml up --build
```
